# Smart PAC Ultra v3.4.1

## اصلاحات فوری UI

- اصلاح کامل خوانده‌شدن فونت از مسیر `ui/fonts/` و همچنین `assets/ui/fonts/`.
- اصلاح خروج نوشته‌ها از دکمه‌های Popup.
- اصلاح آیکون‌های بزرگ/سیاه و تبدیل همه آیکون‌های UI به حالت خطی کنترل‌شده.
- اصلاح نمایش نتیجه تست اتصال در Popup.
- فشرده‌سازی بخش تنظیمات سریع، تب‌ها و کارت‌های وضعیت.
- حفظ ساختار آماده GitHub Release.

> فایل‌های فونت در بسته بازنشر نشده‌اند؛ کاربر باید فایل‌های VazirFD را در مسیر `ui/fonts/` یا `assets/ui/fonts/` قرار دهد.
